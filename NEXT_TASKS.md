# Next Tasks

## 1. Add GitHub Actions Discord notifications

- Goal: post clean PR opened, updated, merged, and closed notices to Discord from GitHub Actions.
- Why: the team needs a durable, low-friction awareness loop for PR activity without depending on manual status checks.
- Needs: the repo secret `DISCORD_WEBHOOK_URL` in GitHub Actions settings.

## 2. Turn Patch 10 into the next focused gameplay slice

- Goal: clearer recap, objective deception, and more room mutation.
- Why: the current prototype already has a strong pressure loop, and the next best value is making the house feel smarter and less predictable.

## 3. Add the first durable playtest note after a real run

- Goal: record what felt confusing, scary, or unreliable in actual play.
- Why: the horror loop will improve faster if the next runner can read one clean summary instead of re-learning from scratch.

## 4. Connect branch protection and PR automation once Discord notifications are live

- Goal: make the bootstrap checks enforceable instead of advisory.
- Why: the repo should not depend on manual memory for basic safety.

## 5. Keep documenting blockers immediately

- Goal: capture any new missing tools, broken references, or external setup requirements as soon as they appear.
- Why: this is the cheapest way to keep unattended runs effective.
